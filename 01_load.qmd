---
title: "01_load"
format: html
editor: visual
---

## Loading libraries (are we sure we need them?)

```{r}
library("tidyverse")
```

## Loading data

```{r}
file_path <- "data/_raw/data.csv"
df <- read.csv(file_path)
#df
```

```{r}
write.csv(df, file = "data/01_dat_load.csv", row.names = FALSE)
```
